import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  title: {
    fontSize: 30,
    fontWeight: 'bold',
    color: '#345F06',
    marginTop: 40,
    textAlign: 'center',
  },
});
